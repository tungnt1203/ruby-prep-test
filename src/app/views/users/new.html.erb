<div class="min-h-screen bg-slate-50 flex items-center justify-center p-4">
  <div class="w-full max-w-md rounded-xl bg-white shadow-sm ring-1 ring-slate-200 p-8">
    <h1 class="text-xl font-semibold text-slate-900">Sign up</h1>
    <p class="mt-2 text-sm text-slate-600">Create an account to join rooms and take exams. Host accounts can be set in the console.</p>

    <%= form_with model: @user, url: signup_path, method: :post, local: true, class: "mt-6 space-y-4" do |f| %>
      <% if @user.errors.any? %>
        <div class="rounded-lg bg-rose-50 p-3 text-sm text-rose-700">
          <%= @user.errors.full_messages.to_sentence %>
        </div>
      <% end %>
      <div>
        <label for="user_email" class="block text-sm font-medium text-slate-700">Email</label>
        <%= f.email_field :email, required: true, autofocus: true, class: "mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-slate-900 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
      </div>
      <div>
        <label for="user_password" class="block text-sm font-medium text-slate-700">Password</label>
        <%= f.password_field :password, required: true, class: "mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-slate-900 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
      </div>
      <div>
        <label for="user_password_confirmation" class="block text-sm font-medium text-slate-700">Password confirmation</label>
        <%= f.password_field :password_confirmation, required: true, class: "mt-1 block w-full rounded-lg border border-slate-300 px-3 py-2 text-slate-900 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
      </div>
      <button type="submit" class="w-full rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-500">Sign up</button>
    <% end %>

    <p class="mt-4 text-sm text-slate-600">
      Already have an account? <%= link_to "Sign in", login_path, class: "text-indigo-600 hover:underline" %>
    </p>
    <p class="mt-2 text-xs text-slate-500">
      <%= link_to "← Back", root_path, class: "text-indigo-600 hover:underline" %>
    </p>
  </div>
</div>
